import { WhitespaceValidator } from './whitespace.validator.ts';

describe('WhitespaceValidator', () => {
  it('should create an instance', () => {
    expect(new WhitespaceValidator()).toBeTruthy();
  });
});
